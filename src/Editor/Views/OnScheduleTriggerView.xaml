<UserControl x:Class="RxdSolutions.FusionScript.Views.OnScheduleTriggerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">
    <Grid>
    
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80px" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Label HorizontalAlignment="Left" VerticalAlignment="Center">Execute at: </Label>

        <xctk:TimePicker VerticalAlignment="Center" Height="25" HorizontalAlignment="Left" Grid.Column="1" Value="{Binding Time, UpdateSourceTrigger=PropertyChanged}" Width="200px" />
    
    </Grid>

</UserControl>
